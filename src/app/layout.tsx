"use client";
import type { Metadata } from "next";
import { Provider } from "react-redux";
import "./globals.css";
import { cn } from "@/lib/utils";

import localFont from "next/font/local";
import background from "../../public/background.png";
import { store } from "@/store/store";

// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
// };

const belezza = localFont({
  src: [
    { path: "../../public/fonts/Belleza/Belleza-Regular.ttf", weight: "400" },
  ],
  variable: "--belleza",
  display: "swap",
  adjustFontFallback: false,
});

const beLoveYaLikeASister = localFont({
  src: [
    {
      // path: "./fonts/Love_Ya_Like_A_Sister/LoveYaLikeASister-Regular.ttf",
      path: "../../public/fonts/Love_Ya_Like_A_Sister/LoveYaLikeASister-Regular.ttf",
      weight: "400",
    },
  ],
  variable: "--beLoveYaLikeASister",
  display: "swap",
  adjustFontFallback: false,
});

const crimson = localFont({
  src: [
    {
      // path: "./fonts/Love_Ya_Like_A_Sister/LoveYaLikeASister-Regular.ttf",
      path: "../../public/fonts/Crimson_Text/CrimsonText-Regular.ttf",
      weight: "400",
    },
  ],
  variable: "--crimson",
  display: "swap",
  adjustFontFallback: false,
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        // className={cn(
        //   "font-sans antialiased bg-[url('/background-chalk1.png')] h-screen bg-cover bg-no-repeat  overflow-y-scroll no-scrollbar",
        //   fontSans.variable
        // )}

        className={cn(
          "antialiased bg-[url('/background-chalk1.png')] h-screen bg-cover bg-no-repeat  overflow-y-scroll no-scrollbar",
          beLoveYaLikeASister.variable,
          crimson.variable,
          belezza.variable
        )}
      >
        <Provider store={store}>{children}</Provider>
      </body>
    </html>
  );
}
